api_key.py
alert_config.py
db_config.py
notes.txt
constants.py
logs
.env
.env.deploy
airflow_env/
# prevent timestamped log files from being committed
*.txt
# local infra secrets (IPs, key file, credentials) — never commit
infra_local.md
# K8s secrets with passwords — never commit
*-secret.yaml
# compiled Python bytecode
__pycache__/
# local development notes
dev_notes_private.md
